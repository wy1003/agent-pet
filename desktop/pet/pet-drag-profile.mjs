export const PET_DRAG_PROFILE = Object.freeze({
  dragThreshold: 4,
  sampleWindowMs: 160,
  minimumSampleIntervalMs: 8,
  minimumSampleDistance: 4,
  minimumFlingSpeed: 320,
  maximumFlingSpeed: 1600,
  frameMs: 16,
  maximumStepMs: 32,
  frictionPerFrame: 0.88,
  bounceFactor: 0.7,
  stopSpeed: 65,
  maximumDurationMs: 900,
});
