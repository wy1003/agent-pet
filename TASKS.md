# Agent Pet 任务清单

> 最后更新：2026-08-03  
> 用途：记录后续开发顺序、验收标准和已经确定的产品决策。  
> 维护规则：新需求先写入本文件；完成后勾选并补充必要说明，不直接删除。

## 当前里程碑：让语音通知真正可日常使用

### P0：播报风格与 Codex 文案池

- [x] 增加称呼、助手自称、表达语气、项目名称和补充风格设置。
- [x] 文案生成方式固定显示为“Codex 自动生成”，不提供手动生成按钮。
- [x] 设置首次加载或风格变化时，由 Codex 在后台静默生成结构化文案池。
- [x] 生成失败时继续使用内置文案，并在后台静默重试。
- [x] 将文案池保存到应用数据目录；风格变化后旧文案池自动失效。
- [x] 使用独立内部工作目录运行文案任务，并按完整路径从任务收件箱中过滤。
- [x] 将文案池接入真实任务事件通知和播报队列。

验收标准：用户无需点击生成按钮；设置页不显示生成过程；Codex 内部文案任务不出现在任务收件箱；没有网络或生成失败时仍有可用提醒文案。

### P0：真实任务事件播报

- [x] 建立统一的通知调度器，消费任务 `created`、`updated` 和 `removed` 事件。
- [x] 只在任务状态发生有效变化时触发通知，避免采集器重复更新造成重复播报。
- [x] 将提醒规则接入以下事件：
  - [x] `needs_input`：需要批准或回答。
  - [x] `completed`：任务完成。
  - [x] `failed`：任务失败。
  - [x] `interrupted`：任务中断。
  - [x] `unknown`：状态未知。
- [x] 接入语音通知总开关。
- [x] 接入免打扰时段和“紧急事件仍然提醒”。
- [x] 根据“简短 / 标准 / 详细”生成自然、长度受控的播报文本。
- [x] Windows 系统语音和 GPT-SoVITS 使用同一套通知调度入口。

验收标准：在 Codex Desktop 和 CC GUI 中分别制造完成、失败和等待输入事件，每个状态只播报一次，关闭规则或处于免打扰时不播报。

### P0：语音播放队列

- [x] 多个任务同时触发时顺序播放，不发生声音重叠。
- [x] 相同任务、相同状态在短时间内去重。
- [x] 应用退出或关闭语音通知时取消待播放内容。
- [x] 支持“新紧急提醒优先于普通完成提醒”。
- [x] 限制队列长度，避免应用长时间离线后集中播报过期消息。
- [x] 将合成失败、播放失败和服务未就绪区分记录到日志。

验收标准：连续完成多个任务时能够依次播报；重复状态更新不重复播报；等待输入不会被一长串普通完成提醒长期阻塞。

### P0：GPT-SoVITS 服务容错

- [x] 启用 GPT-SoVITS 且服务未启动时，按设置自动后台启动。
- [x] CPU 模式启动较慢时等待服务就绪，不立即判定失败。
- [x] 设置合理的启动、合成和播放超时。
- [x] 服务启动失败时保留系统通知，并在设置页展示可理解的诊断信息。
- [ ] 明确决定 GPT-SoVITS 失败时是否回退到 Windows 系统语音。

验收标准：服务停止后首次收到任务事件，应用能够自行恢复服务并播报；失败时不会阻塞任务采集或系统通知。

## 下一阶段

### P0：桌面宠物阶段 A（本地宠物与 Codex 风格拖动）

> 实现依据：`docs/CODEX_PET_IMPLEMENTATION_GUIDE.md`。当前 `badgeWindow` 采用渐进迁移，任务面板、通知和语音继续复用，不新增第二套消息气泡。

- [ ] 建立 `desktop/pet/` 模块边界：动画配置、状态控制、拖动物理和宠物窗口控制不继续堆入 `desktop/main.mjs`。
- [x] 增加 Codex 兼容 v1/v2 精灵渲染器，支持 `8×9`、`8×11` 网格和整数缩放素材。
- [ ] 准备一份项目有权分发的默认宠物包；未准备完成前保留现有 CSS 徽标作为安全回退。
- [x] 将任务 SSE 聚合状态映射到 `idle / running / waiting / review / waving / failed`，一次性状态按 `${taskId}:${status}` 去重。
- [x] 把点击和拖动统一到宠物命中区域：移动达到 `4px` 才算拖动，拖动释放不触发任务面板。
- [x] 新增最近 `160ms` 速度采样、惯性、衰减和工作区边缘反弹；参数集中在纯函数模块并覆盖单元测试。
- [x] 将单一 `badgeBounds` 升级为版本化宠物锚点、显示器 ID 和分辨率回退，并使用原子写入。
- [x] 监听显示器新增、移除和参数变化，保证多屏及负坐标布局下宠物始终可见。
- [x] 将设置版本升级到 9，迁移 `showBadge → showPet`，增加宠物大小、渲染方式、悬停动画、视线跟随、减少动态和甩动开关。
- [x] 任务面板按可见卡片数量动态定位：0～1 张短卡片靠近宠物头部，2 张及以上保持侧边底部对齐。
- [ ] 将 `companion-badge.*` 渐进迁移为 `pet.*`；稳定前保留旧 IPC 和状态字段一个版本，避免一次性破坏现有功能。
- [ ] 在 Windows 100%、125%、150% DPI 下回归点击、拖动、甩动、反弹、面板定位和重启位置恢复。

验收标准：不同任务状态能稳定驱动宠物动画；点击与拖动不串事件；快速甩动可衰减反弹；多屏变化后宠物不会消失；现有任务面板、系统通知和语音提醒不回归。

### P1：桌面宠物阶段 B/C（宠物库、本地导入与 Petdex）

- [ ] 实现宠物包校验、受管宠物库、选择、删除、更新和本地目录导入。
- [ ] 只读发现 `.codex/pets` 与 `.petdex/pets`，用户确认后复制到本项目受管库。
- [ ] 接入 Petdex manifest adapter、缓存、安全下载、取消、哈希和 staging 原子安装。
- [ ] 默认只安装到本项目；“同时安装到 Codex”必须由用户显式勾选。
- [ ] 保存宠物来源、作者、许可和署名信息，不把 Petdex 代码许可当作宠物素材许可。

### P1：通知体验与可诊断性

- [x] 设置页增加“发送测试任务提醒”，一次验证规则、文本生成和播放队列。
- [x] 增加长期保留的通知记录：时间、任务、渠道、结果和失败原因。
- [x] 将通知记录入口收纳到“通用 → 本地数据”，仅显示可点击的受管目录路径，不在设置页渲染历史卡片。
- [x] 隔离桌面版与开发 CLI 的已读状态；“全部已读”单次原子持久化，写入失败时回滚，重启后不再恢复已读任务。
- [ ] 查看日志时能区分应用日志、GPT-SoVITS 服务日志和安装日志。
- [ ] 为“服务正在启动、正在合成、正在播放、播放完成”提供轻量状态反馈。
- [ ] 完善长项目名、长任务标题、特殊符号和中英混合文本的播报清洗。

### P1：一键工作日报

- [x] 在宠物与系统托盘右键菜单增加“总结今日工作”入口。
- [x] 从 `scope=all` 历史中读取设备本地日期内完成的任务，已确认或已读任务仍参与总结。
- [x] 只统计顶层 `completed` 任务，排除子代理与 Agent Pet 内部 Codex 文案任务。
- [x] 使用 Codex 按项目归类并合并同一会话的过程性追问，输出结构化工作成果。
- [x] 提供可编辑、可复制和可重新生成的轻量日报窗口。
- [x] Codex 不可用时自动回退为本地按项目整理的基础日报。
- [x] 日报按本地日期缓存；当天再次打开直接显示已有内容，只有点击“更新日报”才重新生成。
- [x] 单次生成“简要 / 标准 / 完整”三档内容，并记住用户上次选择。
- [ ] 后续接入其他 Agent 时，将各来源的完成事件归一化到长期任务历史。
- [ ] 如需在用户清理 AI 客户端历史后继续保留日报，新增按月归档的完成任务快照。

验收标准：点击“总结今日工作”后，只展示当天真实完成的工作；不重复统计子代理和内部生成任务；生成失败时仍可复制基础日报。

### P1：GPT-SoVITS 安装与环境管理

- [ ] 将安装过程从终端交互升级为应用内向导和进度展示。
- [ ] 支持暂停失败后的继续安装，尽量复用已下载缓存。
- [ ] 显示预计下载量、已用磁盘空间和安装目录。
- [ ] 验证 CPU 与 NVIDIA CUDA 12.6 / 12.8 之间的重配置流程。
- [ ] 卸载语音运行环境时允许用户选择是否保留音色库。
- [ ] 验证卸载后不修改系统 Python、PATH、注册表和 Shell 配置。
- [ ] 为安装文件和关键下载增加来源、版本及校验信息。

### P1：音色库管理

- [ ] 支持导入前校验 `.ckpt`、`.pth`、参考音频和参考文本。
- [ ] 明确显示音色占用空间、参考语言和文件完整性。
- [ ] 支持打开应用管理的音色目录。
- [ ] 支持导出 / 备份用户自己的音色配置。
- [ ] 删除音色前说明删除范围并要求确认。
- [ ] 研究不同 GPT-SoVITS 版本音色模型的兼容性并给出错误提示。

### P1：收件箱与真实场景回归

- [x] 识别任务执行期间提交的排队消息，显示为独立“等待开始”任务，并在真实下一轮启动时接续。
- [x] 同一会话折叠时保留全部运行中、等待输入和排队任务，只折叠已结束的历史任务。
- [x] 使用 `turn_context` 将 CC GUI 的主提示准确归属到对应任务，避免历史任务文本逐轮错位。
- [x] 自动清理超过 24 小时仍未被真实任务接续的孤立排队记录，并清除持久化状态中的悬空任务 ID。
- [ ] 用 Codex Desktop 回归任务创建、运行、等待输入、完成、失败、中断和签收。
- [ ] 用 PyCharm CC GUI 回归同一套生命周期。
- [ ] 验证多个项目、多个会话和同一会话多个任务并行。
- [ ] 检查小窗口、系统缩放 125% / 150% 和长内容布局。
- [ ] 增加关键设置交互的端到端 UI 测试。

## 后续规划

### P2：手机 / 远程通知

- [x] 第一版确定直接接入腾讯 openclaw-weixin 使用的 iLink 协议，不在提醒规则中按渠道拆列。
- [x] 远程通知页改为可连接服务卡片，第一版仅提供微信。
- [x] 远程渠道凭据使用系统安全存储，不以明文写入普通配置。
- [x] 提供扫码、验证码、绑定、连接状态、解除连接与发送测试。
- [x] 真实任务事件接入串行远程投递队列，提供有限重试和失败记录。
- [x] 在 README 明确联网目标、隐私、消息内容和第一版能力边界。
- [ ] 根据真实使用情况增加可配置的主动发送频率限制。

### P2：发布与维护

- [ ] 确定正式产品名、应用图标、版本号与版权信息。
- [ ] 生成 Windows 安装包并验证安装、升级、降级和卸载。
- [ ] 大型 GPT-SoVITS 运行环境继续按需下载，不打进主安装包。
- [ ] 卸载主程序时提供清理运行环境、缓存、日志和音色库的选项。
- [ ] 编写首次使用向导和故障排查文档。
- [ ] 设计自动更新和版本兼容策略。

## 已完成基线

- [x] 采集 Codex Desktop 与 CC GUI 会话，并按 `turn_id` 形成独立任务。
- [x] 提供当前任务收件箱、实时更新、终态保留和手动签收。
- [x] 提供 Electron 徽标、任务面板、托盘和 Windows 系统通知。
- [x] 设置页按提醒规则、系统通知、语音播报、远程通知、外观与启动分类。
- [x] 提醒规则只表达“任务事件是否通知”，不按 Windows、语音、手机拆列。
- [x] Windows 本地语音支持音色、语速、音调、音量和试听。
- [x] GPT-SoVITS 支持独立按需安装、后台服务、自动启动和试听。
- [x] GPT-SoVITS 运行环境位于应用专属目录，不依赖系统 Python 或 Conda。
- [x] 音色导入后复制到应用管理目录，源文件移动或删除不影响使用。
- [x] 音色库使用用户自定义正式名称，不随软件分发第三方角色音色。
- [x] 语音引擎使用 Windows / GPT-SoVITS 两个可见按钮切换。
- [x] 语音设置按本地服务、运行环境、音色库、合成参数、高级连接、播报与试听分组。
- [x] 设置页在窄窗口下无横向溢出，关键控件自动改为单列。
- [x] 后台服务启动不显示 CMD 窗口，设置页可查看运行状态和日志。
- [x] 计算设备设置支持 CPU 与受支持的 NVIDIA CUDA 环境重配置。
- [x] 当前测试基线：103 项通过，0 失败。

## 已确定的产品与技术决策

- 提醒规则描述任务事件，通知渠道在各自设置中独立启用。
- 软件默认不携带、下载或分发第三方角色音色；用户只导入自己有权使用的文件。
- GPT-SoVITS 及其大型依赖按需安装，不增加主安装包体积。
- GPT-SoVITS、Python、依赖、缓存和基础模型放在应用专属目录，便于整体清理。
- 模型与参考音频必须复制到应用音色库，不能长期引用用户原始路径。
- GPT-SoVITS 服务由应用在后台管理，可选择随应用自动启动。
- 服务地址、语言等低频项放在“高级连接”；普通用户主要选择音色和语速。
- 当前 Windows 安装方案仅为 NVIDIA 提供 CUDA 选项；AMD Radeon RX 6800 XT 暂用 CPU 模式，不能误显示 CUDA。
- 第一版远程通知直接接入腾讯 iLink，界面按“可连接的服务”组织；后续渠道继续以卡片扩展，不修改提醒规则的数据模型。
- 播报文案默认由 Codex 后台静默维护；设置页不提供手动生成、进度或失败提示，失败时回退内置文案。
- Codex 文案任务使用应用专属工作目录，并按完整规范化路径排除，避免误隐藏用户同名项目。

## 暂缓事项

- [ ] AMD GPU 原生加速：等待 Windows 下稳定、可维护的 PyTorch / GPT-SoVITS 方案后再评估。
- [ ] 其他 AI 编程工具适配：待当前 Codex 通知链路稳定后再抽象采集 adapter。
- [x] 桌宠动画与复杂聚合状态：已形成独立实现指南并进入阶段 A，不与单项任务状态混用。

## 2026-08-03：GIF 宠物 ZIP 导入第一版

- [x] 支持在“外观与启动 → 宠物库”拖入或选择完整 ZIP 压缩包。
- [x] 校验统一宠物名前缀、11 个标准状态、192×208 尺寸及各状态帧数。
- [x] 使用 staging 原子导入到 Agent Pet 受管数据根的 `pets` 目录，不长期引用源 ZIP；兼容早期 `%LOCALAPPDATA%\CodexTaskCompanion` 数据。
- [x] 支持选择已导入宠物、删除应用受管副本以及打开宠物库目录。
- [x] 桌面宠物根据 `idle / running / waiting / review / waving / failed` 等状态播放对应 GIF。
- [x] 使用用户提供的 `yijian-gifs.zip` 实际验证通过（识别到 `yijian` 和 11 个状态）。
- [x] 当前自动化测试基线：103 项通过，0 项失败。
- [ ] 后续版本再增加显示名称编辑、作者/许可信息和在线 Petdex 导入，不纳入第一版。
